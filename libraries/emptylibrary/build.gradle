apply plugin: 'com.android.library'

android {
    lintOptions {
        disable 'HardcodedDebugMode'
    }
}

dependencies {
    api deps.support.appCompat

    compileOnly deps.support.multidex

    testImplementation deps.test.junit
    testImplementation deps.test.robolectric
}

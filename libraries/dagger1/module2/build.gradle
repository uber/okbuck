apply plugin: 'java-library'
apply plugin: 'net.ltgt.apt'

dependencies {
    api deps.external.dagger1
    apt deps.apt.dagger1Compiler

    // Change this to api or compile to "fix" error.
    implementation project(':libraries:dagger1:module3')
}

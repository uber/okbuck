import re

file_name_regex = re.compile(r'^.*/([^/]+)$', re.IGNORECASE)
files = []

for file in glob(['*']):
  name = file_name_regex.sub(r'\1', file)
  files.append(name)

files.remove('BUCK')

for file in files:
  export_file(
    name = file,
    visibility = ['PUBLIC'],
  )

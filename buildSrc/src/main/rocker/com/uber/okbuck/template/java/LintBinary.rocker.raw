java_binary(
    name='okbuck_lint',
@if (valid(deps)) {
    deps = [
    @for (dep : sorted(deps)) {
        '@dep',
    }
    ],
}
    blacklist=[
        'META-INF/.*\.SF',
        'META-INF/.*\.DSA',
        'META-INF/.*\.RSA',
    ],
    visibility = ['PUBLIC'],
)

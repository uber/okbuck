@args (
String name,
String storeFile,
String storePassword,
String keyAlias,
String keyPassword
)
@(ruleType)(
    name = '@name',
    storeFile = '@storeFile',
    storePassword = '@storePassword',
    keyAlias = '@keyAlias',
    keyPassword = '@keyPassword',
)
